#include bitsstdc++.h
using namespace std;
typedef long long ll;
const ll INF = 1e18;
const ll mod = 1e9+7;

int main(){
    int n, k;
    cinnk;
    vectorint v(n);
    for(int &x v)
        cinx;
    setint s;
    vectorint ans;
    for(int i = 0; i  n; i++){
        if(!s.count(v[i])){
            s.insert(v[i]);
            ans.push_back(i+1);
        }
    }
    if(ans.size()  k)
        coutNOn;
    else{
        coutYESn;
        for(int i = 0; i  k; i++)
            coutans[i] ;
    }
    return 0;
}
